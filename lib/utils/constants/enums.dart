//List of dart
// They cannot be created inside the class
enum ProductType{
  single,
  variable,
}


enum TextSizes {
  small,
  medium,
  large,
}

enum OrderStatus {
  pending,
  processing,
  shipped,
  delivered,
  cancelled,
}

enum PaymentMethod {
  visa,
  masterCard,
  googlePay,
  applePay,
  paytm,
  razorPay,
  creditCard,
  debitCard,
  paypal,
  cashOnDelivery,
}